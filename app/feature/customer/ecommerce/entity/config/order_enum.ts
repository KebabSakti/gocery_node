export enum OrderStatus {
  PENDING = "pending",
  ACTIVE = "active",
  PROGRESS = "progress",
  COMPLETED = "completed",
  CANCELED = "canceled",
}

export enum PaymentStatus {
  UNPAID = "unpaid",
  PENDING = "pending",
  PAID = "paid",
  CANCELED = "canceled",
  EXPIRED = "expired",
}
