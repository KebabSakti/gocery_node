export enum OrderStatus {
  PENDING = "pending",
  PROGRESS = "progress",
  COMPLETED = "completed",
  CANCELED = "canceled",
}

export enum PaymentStatus {
  UNPAID = "unpaid",
  PENDING = "pending",
  PAID = "paid",
  CANCELED = "canceled",
}
